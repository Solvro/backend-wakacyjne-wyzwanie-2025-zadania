# Database Configuration
# For local development, you can use this PostgreSQL connection string
# Make sure to create a local PostgreSQL database named 'budzetownik'
DATABASE_URL="postgresql://user:password@localhost:5432/budzetownik?schema=public"

# Authentication Configuration
# Token expiry time in milliseconds (default: 1 hour)
EXPIRY_TIME_MS=3600000
